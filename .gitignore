# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.aider*
*cache*
aider*

# Rust / WASM build artifacts (the generated src/wasm/pkg IS committed)
wasm/prompt-forge/target

# Live benchmark proofs — non-deterministic + carry per-run USD cost data
bench/*.json

# RuVector local store written at runtime by the harness / app
ruvector.db
**/ruvector.db

